{
  "name": "container-iteration",
  "type": "registry:component",
  "title": "Container Iteration",
  "description": "Container Iteration - 组件",
  "dependencies": [
    "@kesi/client"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "registry/components/kesi/container-iteration/container-iteration.tsx",
      "type": "registry:component",
      "content": "'use client'\n\nimport { ReactNode } from 'react'\nimport { IterationContext } from '@kesi/client'\n\n// Props interface\nexport interface IterationProps {\n  children: ReactNode\n  iterationList: any[]\n}\n\n// Iteration Container Component\nexport function Iteration({\n  children,\n  iterationList = [],\n}: IterationProps) {\n  return (\n    <>\n      {iterationList.map((item, index) => (\n        <IterationContext.Provider key={index} value={{ value: item, index } as any}>\n          {children}\n        </IterationContext.Provider>\n      ))}\n    </>\n  )\n}\n"
    }
  ]
}